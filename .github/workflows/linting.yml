name: Linting

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v3.5.3

      - name: Setup
        run: brew install ktlint

      - name: Linting
        run: ktlint src/android/*.kt
